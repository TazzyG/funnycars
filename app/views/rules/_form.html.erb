<%= form_for(@rule, html: { multipart: true }) do |f| %>
  <% if @rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rule.errors.count, "error") %> prohibited this rule from being saved:</h2>

      <ul>
      <% rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="form-group">
      <%= f.hidden_field :user_id %>
    </div>
    <div class="form-group">
      <%= f.text_field :title, class: "form-control", placeholder: "Title" %>
    </div>
    <div class="form-group">
      <%= f.text_field :number, class: "form-control", placeholder: "Reference Number" %>
    <div class="form-group">
      <%= f.text_field :description, class: "form-control", placeholder: "Details" %>
    </div>
    <div class="form-group">
      <%= f.label :picture %>
      <%= f.file_field :picture, class: "form-control", placeholder: "Select Feature Image" %>
      <%= f.hidden_field :picture_cache %>
    </div>
    
    <div class="btn-group">
      <%= f.submit "Submit rule", class: "btn btn-primary" %>
    </div>
<% end %>

<%= render "shared/image_script" %>

