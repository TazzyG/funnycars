
    <% @announcements.each do |announcement| %> 
      <div class = "row text-left announcement_comments">
        <div class= "col-md-10">
       
          <% if announcement.sticky? %>
            <h1> <%= link_to announcement.title, announcement_path(announcement) %></h1>
            <h2 class= "text-primary"><%= announcement.sub_title %></h2>
            
            <p>
              <% if current_user %>
                <%= link_to "Edit", announcement_path(announcement) %> |
                <%= link_to "Delete", announcement, method: :delete,
                  data: {confirm: "Are you sure?"} %>
              <% end %>
            </p>
            
            <% if announcement.youtube? %> 
              <div class="youtube">
                <iframe width="700" height="394" src="https://www.youtube.com/embed/<%=announcement.youtube%>" frameborder="0" allowfullscreen></iframe>
              </div>
            <% elsif announcement.pdf? %>
             <em><%= link_to announcement.race_schedule.details, race_schedule_path(announcement.race_schedule) %> </em>
             <br>
             <iframe src=<%= announcement.pdf.url %> width="350" height="390" style="border: none;"> </iframe>
            <% end %> 
            
            <p> 
             <%= simple_format(announcement.content) %>
            </p>
            <hr class = "primary">
          <% end %>
        </div>
      </div>
    <% end %> 
    
    <% @announcements.each do |announcement| %> 
      <div class = "row text-left announcement_comments">
        <div class= "col-md-10">
          <% if announcement.sticky == false %>
            <h1> <%= link_to announcement.title, announcement_path(announcement) %></h1>
            <h2 class= "text-primary"><%= announcement.sub_title %>
            <% if current_user %>
              <%= link_to "Edit", announcement_path(announcement) %> |
              <%= link_to "Delete", announcement, method: :delete,
                data: {confirm: "Are you sure?"} %>
            <% end %>
            </h2>
             <% if announcement.youtube? %> 
              <div class="youtube">
                <iframe width="700" height="394" src="https://www.youtube.com/embed/<%=announcement.youtube%>" frameborder="0" allowfullscreen></iframe>
              </div>
            <% elsif announcement.pdf? %>
             <em class= "sponsor img-center img-responsive"><%= link_to announcement.race_schedule.details, race_schedule_path(announcement.race_schedule) %> </em>
             <br>
             <iframe src=<%= announcement.pdf.url %> width="425" height="550" style="border: none;"> </iframe>
            <% end %> 
            
            <p> 
              <%= simple_format(announcement.content) %>
            </p>
            <hr class = "primary">
          <% end %>
        </div>
      </div>
    <% end %>
 